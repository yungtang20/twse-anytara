name: Supabase TDCC sync

on:
  workflow_dispatch:

concurrency:
  group: supabase-cloud-sync
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  sync-tdcc:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Explain local-first TDCC synchronization
        run: |
          echo "Cloud-only TDCC download is disabled."
          echo "Run the local Saturday 18:00 scheduler: official download -> local whitelist -> SQLite -> same batch to Supabase."
